import React from 'react';

const Form = ({onSubmit}) => {
    return (
        <>
            <form action="">
                onSubmit={onSubmit}

            </form>

        </>
    );
};

export default Form;